package settings

import rl "github.com/gen2brain/raylib-go/raylib"

// WINDOW
const (
	WINDOW_HEIGHT = 800
	WINDOW_WIDHT  = 800
)

// COLORS
var (
	DARK_GREY = rl.NewColor(51, 51, 51, 255)
	BEIGE     = rl.NewColor(255, 248, 231, 255)
)

// POINTS
const (
	POINTS_D = 4
)

// MESH
const (
	MESH_GAP_X = 10
	MESH_GAP_Y = 10
)
